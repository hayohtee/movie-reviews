[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "joblib>=1.5.3",
    "nltk>=3.9.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared"]