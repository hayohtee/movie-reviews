[project]
name = "ml-training"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "shared-lib",
    "ipykernel>=7.2.0",
    "numpy>=2.4.4",
    "scikit-learn>=1.8.0",
    "nltk>=3.9.4",
]

[tool.uv.sources]
shared-lib = {path = "../shared-lib", editable = true}
